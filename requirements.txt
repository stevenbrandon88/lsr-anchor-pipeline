# LSR Anchor Pipeline — Verified Dependencies
# Python 3.10-3.12 | Install: pip install -r requirements.txt

# World Bank data
wbgapi>=1.0.12
requests>=2.28.0

# Data
pandas>=1.5.0
numpy>=1.23.0
openpyxl>=3.0.10
scipy>=1.9.0

# Regression / statistics
statsmodels>=0.13.0
scikit-learn>=1.1.0

# Specification curve — Simonsohn et al. 2020
# API: m = OLSRobust(y=[...], x=[...], data=df); m.fit(controls=[...]); r = m.get_results()
robustipy>=0.1.0

# Causal identification — Microsoft Research
dowhy>=0.9.0

# Heterogeneous treatment effects — Microsoft Research
# API: CausalForestDML(discrete_outcome=True, discrete_treatment=True)
econml>=0.14.0

# Visualisation
matplotlib>=3.6.0
seaborn>=0.12.0
